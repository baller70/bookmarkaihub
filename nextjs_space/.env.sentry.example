# ===========================================
# SENTRY ERROR TRACKING & PERFORMANCE MONITORING
# ===========================================
# Get these values from your Sentry project settings:
# 1. Go to https://sentry.io
# 2. Create or select a project
# 3. Go to Settings > Projects > [Your Project] > Client Keys (DSN)
# 4. Copy the DSN value

# Required: Your Sentry DSN (Data Source Name)
# Format: https://[key]@[org].ingest.sentry.io/[project-id]
NEXT_PUBLIC_SENTRY_DSN=https://your-key@o123456.ingest.sentry.io/1234567
SENTRY_DSN=https://your-key@o123456.ingest.sentry.io/1234567

# Required for source map uploads (get from Organization Settings > Auth Tokens)
SENTRY_AUTH_TOKEN=your-auth-token-here

# Your Sentry organization slug (from organization settings URL)
SENTRY_ORG=your-org-slug

# Your Sentry project slug (from project settings URL)
SENTRY_PROJECT=bookmarkhub

# ===========================================
# NOTES:
# ===========================================
# - NEXT_PUBLIC_SENTRY_DSN is used on the client-side (browser)
# - SENTRY_DSN is used on the server-side
# - Both should have the same value
# - SENTRY_AUTH_TOKEN is only needed for source map uploads in CI/CD
# - Without these variables, Sentry will be disabled automatically

